{
  "name": "@fc/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared types and runtime schemas. The seam between both engineers — frozen early, changed only by agreement.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
