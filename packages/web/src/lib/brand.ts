/**
 * The wordmark, in one place. It is set in the nav, scanned in by the intro,
 * and drawn across the footer — so a rename is this line and nothing else.
 */
export const BRAND = 'RECONSTRUCTOR';
