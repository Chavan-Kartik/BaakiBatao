/**
 * The wordmark, in one place. It is set in the nav, scanned in by the intro,
 * and drawn across the footer — so a rename is this line and nothing else.
 *
 * `BRAND` is the display form; `BRAND_MARK` is the all-caps form the intro
 * loader and the footer wordmark draw letter by letter.
 */
export const BRAND = 'BaakiBatao';

export const BRAND_MARK = 'BAAKIBATAO';

/** What the product does, in the fewest words that are still true. */
export const TAGLINE = 'Settlement reconstructor';
