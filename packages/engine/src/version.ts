/**
 * Pinned into every certificate. Bump when a step's arithmetic changes, so an
 * old certificate remains verifiable against the engine that produced it.
 */
export const ENGINE_VERSION = '0.1.0';
