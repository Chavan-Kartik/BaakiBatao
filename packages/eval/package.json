{
  "name": "@fc/eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Corpus generation, fault injection, and the detection-rate harness.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fc/contracts": "workspace:*",
    "@fc/engine": "workspace:*",
    "@fc/fixtures": "workspace:*",
    "@fc/normalise": "workspace:*",
    "@fc/rulepack": "workspace:*",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
